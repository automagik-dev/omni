/**
 * Configuration helpers for the Teams plugin.
 *
 * Group 2 ships a placeholder so the directory exists; per-instance config
 * resolution (DM policy, conversation allowlists, retry tuning) lands as
 * each Group implements its surface.
 */

export {};
