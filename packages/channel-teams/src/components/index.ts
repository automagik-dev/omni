/**
 * Reusable Teams UI components (Adaptive Cards, message extensions).
 *
 * Placeholder — Adaptive Cards and messaging extension surfaces are
 * deferred per the wish scope. Future component builders land here so they
 * mirror the Slack `components/blocks.ts` pattern.
 */

export {};
